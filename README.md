# speedtest-ookla-linux
One-command Bash installer and runner for Ookla Speedtest CLI on Linux.

```
wget https://raw.githubusercontent.com/amirmbn/speedtest-ookla-linux/main/speedtest-ookla.sh && chmod +x speedtest-ookla.sh && sudo ./speedtest-ookla.sh
```
